// The daily/streak leaderboard's Cloudflare Worker — its own D1 database,
// separate from anything else in the tb-dev family.
export const API_BASE = 'https://anniethmetic-leaderboard.tb-dev.workers.dev'
